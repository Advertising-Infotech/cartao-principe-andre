# History 000009 — Correção do .gitignore para ignorar todos os arquivos *.psd

**Data:** 2026-06-24 16:00:00
**Status:** Concluído

---

## Prompt do Usuário
> Verificar se .gitignore ignora *.psd. Se não, corrigir. Apagar arquivos .psd do GitHub. Proceder ritual: history, commit, push.

## O que foi feito

### 1. Verificação do .gitignore
- Linha 38 continha `19.psd` — ignorava apenas um arquivo específico
- **Correção:** Alterado para `*.psd` para ignorar todos os arquivos Photoshop

### 2. Verificação de arquivos .psd rastreados
- `git ls-files "*.psd"` retornou vazio — nenhum arquivo .psd estava rastreado
- Nenhuma remoção necessária no GitHub

## Arquivos modificados
- `.gitignore` — linha 38: `19.psd` → `*.psd`

## Commits
| Hash | Mensagem |
|------|----------|
| (pendente) | Corrigido .gitignore para ignorar todos os arquivos *.psd |
